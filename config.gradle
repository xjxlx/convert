ext {
    // 全局版本配置
    android = [
            compileSdkVersion: 31,
            buildToolsVersion: "30.0.3",
            minSdkVersion    : 30,
            targetSdkVersion : 30,
            versionCode      : 1,
            versionName      : "1.0"
    ]
}